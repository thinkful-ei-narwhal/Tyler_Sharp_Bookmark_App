const BASE_URL = 'https://thinkful-list-api.herokuapp.com/tylersharp/bookmarks';

function mainApi(BASE_URL, ...args) {
  console.log(fetch(BASE_URL));
  
}





const api = {
  //container for all functions
  BASE_URL,
};

export default {
};

